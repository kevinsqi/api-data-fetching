[project]
name = "meter-api"
version = "0.1.0"
description = "Interview coding challenge - meter usage API with rate limiting"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.109.0",
    "uvicorn>=0.27.0",
    "slowapi>=0.1.9",
    "pydantic>=2.0.0",
]

[tool.uv]
dev-dependencies = []
